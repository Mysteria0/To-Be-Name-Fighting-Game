// this file only read once while enable plugin (or restart editor)

// light weight mode disable inspector plugin,
// which force the Parser Node use Array as input interface, make it hard to use
// NOT recommand turn on this function while need to setup new parser
// However, once you have done the basic setup,
// than you can turn this on to reduce cost of running this plugin
light_weight_mode = false
